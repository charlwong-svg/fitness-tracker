import React from "react";
import ReactDOM from "react-dom/client";
import MyFitnessTracker from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MyFitnessTracker />
  </React.StrictMode>
);
