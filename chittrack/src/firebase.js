import { initializeApp } from "firebase/app"; import { getAuth } from "firebase/auth"; import { initializeFirestore, persistentLocalCache, persistentSingleTabManager } from "firebase/firestore"; import { firebaseConfig } from "./firebaseConfig.js"; export const firebaseEnabled = firebaseConfig.apiKey !== "YOUR_API_KEY"; let app, auth, db; if (firebaseEnabled) { app = initializeApp(firebaseConfig); auth = getAuth(app); try { db = initializeFirestore(app, { localCache: persistentLocalCache({ tabManager: persistentSingleTabManager() }), }); } catch { db = initializeFirestore(app, {}); } } export { auth, db };